.env
.env.local
.env.*.local
.psst/
*.pem
*.key
id_rsa
id_ed25519
# demo runtime state: tui-habits writes its data file next to the binary
demos/tui-habits/habits.json
# matrix run noise: raw session transcripts (usage.json is the committed summary) + py caches
evals/matrix-runs/*/.sessions/
__pycache__/
*.pyc
# demoscene render products (mp4/gif/pgm build outputs, regenerate via mk-*.py)
demoscene/out/
# mock demo run products (regenerate: make demo-mock)
demos/mock/build/
demos/mock/src/

# pi session logs — runtime data, keep local only
evals/matrix-runs/**/.sessions/
*.tgz
